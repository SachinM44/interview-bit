/// so here we will have consumer and the provider componets 

import React, { useState } from "react";



export const UserContext = React.useContext();


export const App = () => {
    const [user, setUse] = useState({ name: 'mai', role: 'amdin' })
    return (
        <userContext.provider value={{ name: "sachin", role: 'aded0', }}>
            <boruwrdROunter>
                <router>
                    route
                </router>
            </boruwrdROunter>

        </Head>

        </userContext >
    )
}


/// so the 